var fileName;
var variables = [{
    type:'input',
    name:'name',
    message:"What is the file's name?"
},
{
    type:'input',
    name:
}
];
// TODO: Include packages needed for this application
const fs = require('fs');
const inquirer = require('inquirer')

// TODO: Create an array of questions for user input
const asks = ["project name","What is your name?","What is your GitHub name?","What is the objective of this application?", "contributers"];

// TODO: Create a function to write README file
function writeToFile(fileName, variables) {
    
}

// TODO: Create a function to initialize app
function init() {
 
}

// Function call to initialize app
init();
